<view class='banner'>
  <swiper bindanimationfinish="swiperChangeTo" indicatorDots='{{indicatorDots}}' current='{{current}}' autoplay='{{autoplay}}' interval='{{interval}}' duration='{{duration}}' circular='{{circular}}' display-multiple-items='1' next-margin='35rpx' previous-margin='35rpx'>
    <block wx:for="{{imgUrls}}" wx:key wx:for-item="k">
      <swiper-item>
        <view class='box'>
          <image class='img1' src='{{k}}' mode="widthFix" lazy-load="true"></image>
        </view>
      </swiper-item>
    </block>
  </swiper>
  <view class="dots">
    <block wx:for="{{imgUrls}}" wx:key>
      <view class="dot{{index == currentSwiper ? ' active' : ''}}">{{current+1}}/{{imgUrls.length}}</view>
    </block>
  </view>
</view>
<view class='content'>
  <view class='title'>{{detailCent.SheetName}}</view>
  <view class='contents'>
    <view class='time'>活动截止时间：{{detailCent.EndTime}}</view>
    <view class='money'>
      <view class='number'>￥{{detailCent.SheetPrice}}</view>
    </view>
  </view>
</view>
<!-- <view class='title'>
  共{{detailCent.TotalItemCount}}门课程，<text>最少{{detailCent.MinItemCount}}门，</text><text>最多{{detailCent.MaxItemCount}}门，</text>仅需{{detailCent.MaxPrice}}元!</view> -->
<view class='courseList'>
  <view class='courseItem' wx:for='{{GroupList}}' wx:key>
    <view class='title'>
      <view class='heng'></view> {{item.GroupName}}({{item.text}})
      <view class='heng'></view>
    </view>
    <checkbox-group bindchange='checkedTap' data-index='{{index}}'>
      <view class='courseCont' wx:for="{{item.ItemList}}" wx:key wx:for-item="k" wx:for-index='index1'>
        <view class='leftImg' data-Id="{{k.RelId}}" wx:key bindtap='courseDetail'>
          <image class='cover' src='{{k.ItemCoverImg}}' mode='widthFix' lazy-load='true'></image>
        </view>
            <view>
              <checkbox class='absolute' value='{{k.RelId}}' checked='{{k.checked}}' wx:if="{{item.MaxCount>1}}"></checkbox>
              <checkbox class='absolute' value='{{k.RelId}}' checked='{{item.checked[0]==k.RelId}}' wx:else></checkbox>
            </view>
        <view class='rightContent' data-Id="{{k.RelId}}" wx:key bindtap='courseDetail'>
          <view class='between relative'>
            <view class='courseTitle'>{{k.ItemName}}</view>
          </view>
          <view class='detail'>
            <image class='detailImg' src='../../images/house.png' mode='widthFix' lazy-load='true'></image>
            <view class='name'>{{k.SellerName}}</view>
          </view>
          <view class='detail around'>
            <view class='addrLong'>
              <image class='detailImg' src='../../images/ad1.png' mode='widthFix' lazy-load='true'></image>
              <view class='name address'>{{k.StoreAddress}}</view>
            </view>
            <!-- <view class='long'>{{k.Distance}}km</view> -->
          </view>
          <view class='detail'>
            <image class='detailImg' src='../../images/call.png' mode='widthFix' lazy-load='true'></image>
            <view class='name'>{{k.Mobile}}</view>
          </view>
          <view class='detail'>
            <view class='ring'>
              <image class='detailImg' src='../../images/couse.png' mode='widthFix' lazy-load='true'></image>
              <view class='name'>共有{{k.CoursesNumber}}节课</view>
            </view>
            <view class='ring'>
              <image class='detailImg' src='../../images/time.png' mode='widthFix' lazy-load='true'></image>
              <view class='name'>单节课时 {{k.SingleTime}} min</view>
            </view>
          </view>
          <view class='tagList'>
            <view class='tagItm' wx:for="{{k.TagList}}" wx:key wx:for-item="l" style='background:{{l.TagBgColor}};color:{{l.TagFontColor}};border:2rpx solid {{l.BorColor}}'>{{l.TagName}}</view>
          </view>
          <view class='coursePeople'>{{k.SoldCount}}人已参与 | 剩余名额 {{k.RemainCount}} 个</view>
        </view>
      </view>
    </checkbox-group>
  </view>
</view>
<view class='pay between'>
  <!-- <view class='el' wx:if="{{Remark==''}}">
    <view class='total'>共{{detailCent.TotalItemCount}}门课程</view>
  </view> -->
  <view class='kl'>
    <view class='total'>共{{detailCent.TotalItemCount}}门课程</view>
    <view class='again' wx:if="{{detailCent.MinItemCount!=detailCent.MaxItemCount}}">您可任意选择{{detailCent.MinItemCount}}到{{detailCent.MaxItemCount}}门</view>
    <view class='again' wx:else>您可任意选择{{detailCent.MaxItemCount}}门</view>
  </view>
  <view class='flex-end'>
    <view class='totalNumber' wx:if="{{TotalPrice==-1}}">合计:暂无价格</view>
    <view class='totalNumber start' wx:else>合计:<view class='payMoney start'><view class='symbol'>￥</view>{{TotalPrice}}</view></view>
    <button class='paybtn' bindtap='paybtn' disabled='{{TotalPrice==-1 ||TotalPrice==0}}'>确认购买</button>
  </view>
</view>